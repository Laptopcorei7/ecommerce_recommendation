[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    trained: needs the real trained model in ml_api/artifacts/; skipped without it
filterwarnings =
    ignore::scipy.sparse.SparseEfficiencyWarning
